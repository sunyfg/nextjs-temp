-- Rename tables to lowercase with underscore separators
RENAME TABLE `User` TO `users`;
RENAME TABLE `Account` TO `accounts`;
RENAME TABLE `Session` TO `sessions`;
RENAME TABLE `VerificationToken` TO `verification_tokens`;
RENAME TABLE `Post` TO `posts`;
